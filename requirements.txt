feedparser==6.0.11
requests==2.32.3
rapidfuzz==3.9.7
Jinja2==3.1.4
PyYAML==6.0.2
python-dateutil==2.9.0.post0
google-generativeai==0.8.3
# Talks to Bedrock via AWS's own Converse API (model-agnostic — Nova,
# Claude, Llama, etc. — see extraction.py's _call_bedrock), not the
# Anthropic SDK's Bedrock client, which only ever worked with Claude.
boto3==1.43.91
